export default class HolbertonClass {
    constructor(size, location) {
        this._size = size;
        this._location = location;
    }

    if (typeof new HolbertonClass() === "string") {
        return this._location;
    }
    return this._size;
}
